// app/(admin)/admin/page.tsx (placeholder para no romper la ruta)
export default function AdminHomePage() {
  return <p className="text-sm text-muted-foreground">Buscador de AniList e importación — próximo paso.</p>
}